# Execution Routing Playbook

Use this playbook to document mature workflows that can skip Codex planning.

Template:

```text
Tool id:
Trigger examples:
Required inputs:
Output shape:
When not to use:
Fallback route:
```

Only mark a workflow as fast lookup when its inputs and outputs are stable.
